# 脚本使用说明

本目录包含用于管理多语言文档翻译的自动化脚本。

## 📋 目录

- [主要脚本](#主要脚本)
- [快速开始](#快速开始)
- [工作流程](#工作流程)
- [多语言扩展](#多语言扩展)
- [脚本说明](#脚本说明)
- [故障排除](#故障排除)

## 主要脚本

### 繁体转简体翻译

#### `translate.js` ⭐ 推荐

统一的繁体中文到简体中文翻译脚本，整合了所有相关功能。

**功能：**
- 使用 Google Translate API 进行语义翻译（繁体 → 简体）
- 修复剩余的繁体字（直接字符替换）
- 智能处理 JSX 标签、代码块等
- 保护产品名称不被翻译

**使用方法：**
```bash
# 完整流程（推荐）
node scripts/translate.js all

# 只执行语义翻译
node scripts/translate.js semantic

# 只修复剩余的繁体字
node scripts/translate.js fix
```

### 多语言翻译

#### `translate-multi-lang.js` ⭐ 新增

支持任意语言之间的翻译，用于添加新语言支持。

**功能：**
- 支持任意语言之间的翻译
- 自动处理文件结构
- 保护产品名称和代码块
- 自动更新路径引用

**使用方法：**
```bash
# 从英文翻译到日文
node scripts/translate-multi-lang.js en ja ja

# 从繁体中文翻译到韩文
node scripts/translate-multi-lang.js zh-TW ko ko

# 从简体中文翻译到英文
node scripts/translate-multi-lang.js zh-CN en en
```

**支持的语言：**
- 英文 (en)
- 繁体中文 (zh-TW, zh-Hant)
- 简体中文 (zh-CN, zh-Hans)
- 日文 (ja)
- 韩文 (ko)
- 西班牙文 (es)
- 法文 (fr)
- 德文 (de)
- 葡萄牙文 (pt)
- 俄文 (ru)

### 英文翻译

#### `translate-all-untranslated.js` ⭐ 推荐

翻译所有未翻译的英文内容。这是主要的英文翻译脚本，功能最完整。

**功能：**
- 智能识别需要翻译的内容
- 自动跳过 JSX 属性、代码块、CSS 属性等不应该翻译的内容
- 保护产品名称不被翻译
- 翻译标题、段落等文本内容
- 使用免费的 Google Translate 服务（无需 API Key）

**使用方法：**
```bash
node scripts/translate-all-untranslated.js
```

#### `translate-manager.js`

管理翻译文件结构，创建缺失的文件并更新链接。

**功能：**
- 扫描所有 `.mdx` 文件
- 创建缺失的繁体中文文件
- 更新内部链接
- 生成翻译状态报告

**使用方法：**
```bash
node scripts/translate-manager.js
```

### 检查脚本

#### `check-untranslated.js`

检查未翻译的英文内容。

**使用方法：**
```bash
node scripts/check-untranslated.js
```

#### `check-i18n.js`

检查多语言配置。

**使用方法：**
```bash
node scripts/check-i18n.js
```

### 修复脚本

#### `fix-translation-errors.js` ⭐ 综合修复脚本

修复所有翻译相关的错误。这是一个综合脚本，包含以下功能：

**功能：**
- 修复被错误翻译的 JSX 标签（如 `<手風琴>` → `<Accordion>`）
- 修复被翻译的 JSX 属性（如 `風格` → `style`）
- 修复产品名称占位符（如 `__產品_1__` → `superun`）
- 修复路径大小写问题
- 修复引号问题（全角引号 → 半角引号）
- 修复其他翻译错误

**使用方法：**
```bash
node scripts/fix-translation-errors.js
```

#### `fix-remaining-traditional.js`

修复剩余的繁体字（直接字符替换）。被 `translate.js` 调用，也可单独使用。

**使用方法：**
```bash
node scripts/fix-remaining-traditional.js
```

### 工具脚本

#### `generate-llms-txt.js`

生成 `llms.txt` 文件，包含所有文档链接。

**使用方法：**
```bash
node scripts/generate-llms-txt.js
```

## 快速开始

### 繁体转简体翻译

```bash
# 完整流程（推荐）
node scripts/translate.js all
```

### 英文翻译

```bash
# 翻译所有未翻译的英文内容
node scripts/translate-all-untranslated.js
```

### 添加新语言

```bash
# 从英文翻译到日文
node scripts/translate-multi-lang.js en ja ja

# 从繁体中文翻译到韩文
node scripts/translate-multi-lang.js zh-TW ko ko
```

## 工作流程

### 1. 添加新页面

```bash
# 1. 创建文件结构并更新链接
node scripts/translate-manager.js

# 2. 翻译内容
node scripts/translate-all-untranslated.js

# 3. 检查翻译状态
node scripts/check-untranslated.js
```

### 2. 繁体转简体

```bash
# 执行完整翻译流程
node scripts/translate.js all
```

### 3. 修复翻译错误

如果翻译后出现解析错误：

```bash
# 运行综合修复脚本
node scripts/fix-translation-errors.js
```

### 4. 验证配置

```bash
# 检查多语言配置
node scripts/check-i18n.js
```

## 多语言扩展

### 添加新语言支持

#### 步骤 1：翻译文档

```bash
# 选择源语言和目标语言
node scripts/translate-multi-lang.js <source-lang> <target-lang> <target-dir>

# 示例：
node scripts/translate-multi-lang.js en ja ja    # 英文 → 日文
node scripts/translate-multi-lang.js zh-TW ko ko # 繁体中文 → 韩文
```

#### 步骤 2：更新 docs.json

在 `docs.json` 中添加新语言配置：

```json
{
  "languages": [
    { "name": "English", "code": "en" },
    { "name": "繁體中文", "code": "zh-Hant" },
    { "name": "简体中文", "code": "zh-Hans" },
    { "name": "日本語", "code": "ja" },      // 新增
    { "name": "한국어", "code": "ko" }       // 新增
  ],
  "og:locale:alternate": "zh-Hant, zh-Hans, ja, ko"  // 更新
}
```

#### 步骤 3：更新导航结构

在 `docs.json` 中为每种语言添加导航结构（参考现有的 `zh-Hant` 和 `zh-Hans` 配置）。

#### 步骤 4：更新 generate-llms-txt.js（可选）

如果需要将新语言文档添加到 `llms.txt`，更新 `generate-llms-txt.js`。

### 添加更多语言支持

要添加新语言，编辑 `translate-multi-lang.js`：

```javascript
// 1. 在 LANG_CODES 中添加语言代码映射
const LANG_CODES = {
  // ... 现有语言
  'it': 'it',           // 意大利文
  'nl': 'nl',           // 荷兰文
  'pl': 'pl',           // 波兰文
};

// 2. 在 LANG_NAMES 中添加语言名称
const LANG_NAMES = {
  // ... 现有语言
  'it': '意大利文',
  'nl': '荷兰文',
  'pl': '波兰文',
};
```

## 脚本说明

| 脚本 | 用途 | 状态 |
|------|------|------|
| `translate.js` | 繁体转简体翻译（统一脚本） | ✅ 推荐 |
| `translate-multi-lang.js` | 多语言翻译（任意语言） | ✅ 推荐 |
| `fix-remaining-traditional.js` | 字符替换映射表 | ✅ 保留 |
| `translate-all-untranslated.js` | 英文翻译脚本 | ✅ 推荐 |
| `translate-manager.js` | 文件结构管理 | ✅ 推荐 |
| `check-untranslated.js` | 检查未翻译内容 | ✅ 推荐 |
| `check-i18n.js` | 检查配置 | ✅ 推荐 |
| `fix-translation-errors.js` | 综合修复脚本 | ⚠️ 需要时使用 |
| `generate-llms-txt.js` | 生成 llms.txt | ✅ 推荐 |

## 注意事项

1. **翻译脚本**会自动跳过不应该翻译的内容（JSX 属性、代码块、CSS 属性等）
2. **修复脚本**应在翻译后运行，以修复可能的解析错误
3. **检查脚本**用于验证翻译状态和配置
4. **API 限制**：Google Translate API 有请求频率限制，脚本会自动添加延迟
5. **网络要求**：需要能够访问 `translate.googleapis.com`
6. **备份建议**：首次运行前建议备份目标目录
7. **执行时间**：完整翻译 92 个文件大约需要 30-60 分钟

## 故障排除

### 语言切换器不显示

1. **检查配置**
   ```bash
   node scripts/check-i18n.js
   ```

2. **检查 Mintlify 版本**
   ```bash
   mint --version
   ```
   确保版本 >= 4.0

3. **重启开发服务器**
   ```bash
   # 停止当前服务器 (Ctrl+C)
   mint dev
   ```

### 翻译后页面解析错误

运行修复脚本：
```bash
node scripts/fix-translation-errors.js
```

这会修复：
- JSX 标签被错误翻译
- JSX 属性被翻译
- 产品名称占位符未还原
- 路径大小写问题
- 引号格式问题
- 其他翻译错误

### API 返回 HTML 错误

如果看到 "API返回HTML，可能是请求过快"：
- 脚本会自动重试
- 可以稍后再次运行 `node scripts/translate.js fix` 修复剩余部分

### 某些繁体字没有被翻译

运行修复脚本：
```bash
node scripts/translate.js fix
```

如果还有遗漏，可以手动添加到 `fix-remaining-traditional.js` 的 `REPLACEMENTS` 或 `CHAR_REPLACEMENTS` 中。

### 切换语言后页面 404

- 确保对应的语言文件存在
- 运行 `node scripts/translate-manager.js` 创建缺失的文件

## 脚本整合说明

### 已整合的脚本

`translate.js` 整合了以下脚本的功能：

| 原脚本 | 功能 | 状态 |
|--------|------|------|
| `translate-traditional-to-simplified.js` | 语义翻译 | ✅ 已整合（已删除） |
| `fix-remaining-traditional.js` | 字符替换 | ✅ 已整合（保留作为映射表） |
| `convert-all-to-simplified.js` | 字符映射 | ⚠️ 已废弃（已删除） |

### 已删除的脚本

以下脚本已被删除（功能已整合或替代）：
- `convert-all-to-simplified.js` - 已被 `fix-remaining-traditional.js` 替代
- `convert-to-simplified.js` - 已被 `fix-remaining-traditional.js` 替代
- `translate-traditional-to-simplified.js` - 已整合到 `translate.js`

## 更多信息

查看根目录的 `TRANSLATION_GUIDE.md` 获取完整的翻译指南。
